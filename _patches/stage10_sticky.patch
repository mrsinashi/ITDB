diff --git a/DESIGN.md b/DESIGN.md
index 0ff71ba..7284039 100644
--- a/DESIGN.md
+++ b/DESIGN.md
@@ -111,7 +111,7 @@ ITDB — рабочий инструмент учёта: на экране мн
 
 | Состояние | Как выглядит |
 |---|---|
-| Строка под курсором | бежевый `--row-hover-bg` `#f9e6ce` во всех таблицах и списках |
+| Строка под курсором | бежевый `--row-hover-bg` `#f9e6ce` во всех таблицах и списках; вертикальные разделители внутри строки — `--row-hover-line`, блочки с числами — `--row-hover-chip` (боковые и горизонтальные линии не меняются) |
 | Сохранено | зелёная вспышка 1.4 с слоем поверх фона (inset-тень + `@property --flash`), гаснет в реальный фон ячейки |
 | Сохраняется | новое значение уже в ячейке, приглушено (opacity .55) |
 | Дубль | красный фон `--dup-bg` |
@@ -134,14 +134,18 @@ ITDB — рабочий инструмент учёта: на экране мн
 - **Рамка блока** (таблица Дерева, История, блоки Справочников,
   пользовательские поля) — единая техника: блок `overflow: hidden` +
   скругление, а сама линия рисуется псевдоэлементом `::after` ПОВЕРХ
-  содержимого, цвет — переменная `--frame`. Так фон шапки уходит под линию
+  содержимого, цвет — переменная `--frame`. Линия — «кольцо» из градиента
+  под маской: на высоте шапки (`--head-h`) она цвета шапки (`--frame-head`:
+  цвет схемы при акцентной шапке), ниже — `--frame`; поэтому серая рамка не
+  обводит цветную шапку. Так фон шапки уходит под линию
   (нет светлого просвета у дуги), рамку не перекрывают строки и углы полосы
   прокрутки. Ничего не должно выходить за рамку: выносные элементы (плашка
   действий дерева) ставятся снаружи блока, в обёртке.
 - **Таблица-блок** (Дерево, История): серая (или акцентная) шапка 24px, сетка
   на всех ячейках, строки-группы `--gray-group` с линиями сверху и снизу
-  темнее обычных (`--group-line`) и без вертикальных разделителей. Если блок
-  прокручивается — рамка у обёртки области прокрутки, шапка липкая внутри.
+  темнее обычных (`--group-line`) и без вертикальных разделителей. Прокрутка —
+  внутри рамки (обёртка с рамкой > область прокрутки); шапка липкая, в Дереве
+  под ней закрепляется текущий адрес, в Истории — текущий день.
 - **Плашка действий строки** (дерево) — одна на блок, справа снаружи рамки у
   строки под курсором; строка при этом остаётся подсвеченной.
 - **Блоки Справочников** — вертикальные, в порядке столбцов таблицы, сеткой
diff --git a/frontend/static/app.css b/frontend/static/app.css
index 26b33a9..81258a3 100644
--- a/frontend/static/app.css
+++ b/frontend/static/app.css
@@ -17,6 +17,7 @@
     --gray-group: #e9e9e9;          /* строка-группа: адрес в дереве, день в истории, группа карточки — везде одна */
     --gray-line: #d8d8d8;           /* линии шапок и групп, рамки блоков */
     --group-line: #c8c8c8;          /* линии над и под строкой-группой (адрес в дереве) */
+    --row-hover-chip: #efd3ae;      /* блочок с числом в строке под курсором — в тон бежевой подсветки */
     --radius: 3px;                  /* единое скругление: как у кнопок и поиска на панели */
     --copy-ico: #b98a4e;            /* значок копирования — в тон бежевой подсветки строки */
 
@@ -1342,8 +1343,11 @@ thead .checkbox-col {
 
 .tree {
     flex: 1;
-    overflow: auto;
-    padding: 10px 12px 30px;
+    min-height: 0;
+    display: flex;
+    flex-direction: column;
+    overflow: hidden;
+    padding: 10px 12px 12px;
     font-size: 13px;
 }
 
@@ -1359,11 +1363,28 @@ thead .checkbox-col {
 .st-block,
 .fd-panel {
     --frame: var(--gray-line);
+    --frame-head: var(--frame);     /* цвет рамки на высоте шапки */
+    --head-h: 24px;                 /* высота шапки блока вместе с её нижней линией */
     position: relative;
     z-index: 0;
     border-radius: var(--radius);
     overflow: hidden;
 }
+.st-block,
+.fd-panel {
+    --head-h: 25px;
+}
+/* Акцентная шапка: боковые и верхняя линии рамки на высоте шапки — цвета
+   шапки, иначе серая рамка вокруг цветной шапки делает её «на пиксель уже»
+   и портит углы. */
+.tree-grid.accent-head,
+.history-frame.accent-head,
+.choices-page.accent-head .st-block,
+.choices-page.accent-head .fd-panel {
+    --frame-head: var(--brand);
+}
+/* Кольцо рамки: фон-градиент (шапка / остальное), вырезанный маской по
+   внутреннему краю — так у рамки два цвета и сохраняются скругления */
 .tree-grid::after,
 .history-frame::after,
 .st-block::after,
@@ -1371,8 +1392,12 @@ thead .checkbox-col {
     content: "";
     position: absolute;
     inset: 0;
-    border: 1px solid var(--frame);
+    border: 1px solid transparent;
     border-radius: var(--radius);
+    background: linear-gradient(to bottom, var(--frame-head) 0 var(--head-h), var(--frame) var(--head-h)) border-box;
+    -webkit-mask: linear-gradient(#000 0 0) padding-box, linear-gradient(#000 0 0);
+    -webkit-mask-composite: xor;
+    mask: linear-gradient(#000 0 0) padding-box exclude, linear-gradient(#000 0 0);
     pointer-events: none;
     z-index: 10;
 }
@@ -1382,9 +1407,28 @@ thead .checkbox-col {
    (computeTreeWidth); справа — место под плашку действий. */
 .tree-wrap {
     position: relative;
+    flex: 0 1 auto;
+    min-height: 0;
+    display: flex;
+    flex-direction: column;
     margin-right: 190px;
 }
+.tree-grid {
+    min-height: 0;
+    display: flex;
+    flex-direction: column;
+}
+/* Прокрутка — внутри рамки, как в Истории */
+.tree-scroll {
+    flex: 0 1 auto;
+    min-height: 0;
+    overflow-x: hidden;
+    overflow-y: auto;
+}
 .tg-head {
+    position: sticky;
+    top: 0;
+    z-index: 4;
     display: flex;
     height: 24px;
     background: var(--grid-head-bg);
@@ -1434,7 +1478,11 @@ thead .checkbox-col {
 
 /* Адрес — строка-группа: серая, линии сверху и снизу темнее обычных,
    без вертикальной линии перед числом */
+/* Текущий адрес закрепляется под шапкой, пока видны его строки */
 .node-row.kind-building {
+    position: sticky;
+    top: 24px;
+    z-index: 3;
     background: var(--gray-group);
     border-top: 1px solid var(--group-line);
     border-bottom-color: var(--group-line);
@@ -1586,6 +1634,26 @@ thead .checkbox-col {
 .node-row.is-hover {
     background: var(--row-hover-bg);
 }
+/* Под курсором вертикальный разделитель и блочок числа — в тон подсветки,
+   как в главной таблице */
+.node-row:hover .node-count,
+.node-row.is-hover .node-count {
+    box-shadow: inset 1px 0 0 var(--row-hover-line);
+}
+.node-row.kind-room:hover .node-count,
+.node-row.kind-room.is-hover .node-count,
+.node-row.kind-building:hover .node-count,
+.node-row.kind-building.is-hover .node-count {
+    box-shadow: none;
+}
+.node-row:hover .node-count .cnt:not(.zero),
+.node-row.is-hover .node-count .cnt:not(.zero) {
+    background: var(--row-hover-chip);
+}
+.node-row.kind-building:hover .node-count .cnt,
+.node-row.kind-building.is-hover .node-count .cnt {
+    background: transparent;
+}
 
 /* Форма добавления/правки — строкой под узлом, со сдвигом его детей */
 .tree-form {
@@ -1721,6 +1789,10 @@ thead .checkbox-col {
 }
 .history-table tbody tr:not(.h-day):hover td {
     background: var(--row-hover-bg);
+    border-right-color: var(--row-hover-line);
+}
+.history-table tbody tr:not(.h-day):hover td:last-child {
+    border-right-color: transparent;
 }
 .history-table .h-time,
 .history-table .h-user {
@@ -1907,6 +1979,9 @@ thead .checkbox-col {
 .st-val {
     flex: 1;
     min-width: 0;
+    /* запас слева под блочок: обрезка по его краю, а не по тексту */
+    padding-left: 5px;
+    margin-left: -5px;
     overflow: hidden;
     white-space: nowrap;
     text-overflow: ellipsis;
@@ -1918,7 +1993,7 @@ thead .checkbox-col {
     max-width: 100%;
     box-sizing: border-box;
     padding: 0 5px;
-    margin-left: -2px;
+    margin-left: -5px;
     border-radius: var(--radius);
     line-height: 17px;
     overflow: hidden;
@@ -2057,9 +2132,11 @@ thead .checkbox-col {
 }
 .fd-head {
     display: flex;
-    align-items: baseline;
+    align-items: center;
     gap: 10px;
-    padding: 4px 8px;
+    height: 25px;
+    box-sizing: border-box;
+    padding: 0 8px;
     background: var(--grid-head-bg);
     border-bottom: 1px solid var(--grid-head-line-h);
 }
diff --git a/frontend/static/app.js b/frontend/static/app.js
index bf3860f..f9b33a2 100644
--- a/frontend/static/app.js
+++ b/frontend/static/app.js
@@ -467,6 +467,7 @@ const app = Vue.createApp({
             treeQuery: "",
             treeWidth: 480,
             treeHover: null,
+            treeScrollbar: 0,
 
             // История
             historyLoading: false,
@@ -1971,6 +1972,7 @@ const app = Vue.createApp({
                 const data = await response.json();
                 this.treeRoots = data.roots || [];
                 this.treeHover = null;
+                this.$nextTick(() => this.watchTreeScroll());
                 this.unlocated = data.unlocated || 0;
                 this.buildTreeIndex();
             } catch (e) {
@@ -2015,6 +2017,30 @@ const app = Vue.createApp({
             this.treeHover = { id: node.id, node: node, top: r.top - w.top, height: r.height };
         },
 
+        // Когда у дерева появляется полоса прокрутки, колонка расширяется
+        // на её ширину — текст узлов не обрезается
+        watchTreeScroll() {
+            const el = this.$refs.treeScroll;
+            const content = this.$refs.treeContent;
+            if (!el || !content || this._treeObservedEl === el) {
+                return;
+            }
+            if (this._treeObserver) {
+                this._treeObserver.disconnect();
+            }
+            const update = () => {
+                const sb = el.offsetWidth - el.clientWidth;
+                if (sb !== this.treeScrollbar) {
+                    this.treeScrollbar = sb;
+                }
+            };
+            this._treeObserver = new ResizeObserver(update);
+            this._treeObserver.observe(el);
+            this._treeObserver.observe(content);
+            this._treeObservedEl = el;
+            update();
+        },
+
         treeAction(method) {
             const hover = this.treeHover;
             this.treeHover = null;
diff --git a/frontend/static/index.html b/frontend/static/index.html
index 09469af..55fd691 100644
--- a/frontend/static/index.html
+++ b/frontend/static/index.html
@@ -210,13 +210,18 @@
             <div class="tree">
                 <!-- Дерево — таблица «Расположение | ПК» в манере главной таблицы.
                      Ширина — по самому широкому узлу с учётом сдвига, даже свёрнутому. -->
-                <div class="tree-wrap" :style="{ width: treeWidth + 'px' }" @mouseleave="treeHover = null">
+                <div class="tree-wrap" :style="{ width: (treeWidth + treeScrollbar) + 'px' }" @mouseleave="treeHover = null">
                     <div class="tree-grid" :class="{ 'accent-head': accentHeaders.tree }">
-                        <div class="tg-head"><span class="tg-name">Расположение</span><span class="tg-count">ПК</span></div>
-                        <tree-form v-if="treeForm && treeForm.top" :level="0"></tree-form>
-                        <div v-if="!treeLoading && !treeError && treeRoots.length === 0" class="tree-empty">Расположения не найдены.</div>
-                        <div v-else-if="treeQuery.trim() && treeMatch.size === 0" class="tree-empty">Ничего не найдено.</div>
-                        <tree-node v-for="root in treeRoots" :key="root.id" :node="root" :level="0"></tree-node>
+                        <!-- Прокрутка внутри рамки: шапка и текущий адрес закрепляются сверху -->
+                        <div class="tree-scroll" ref="treeScroll" @scroll="treeHover = null">
+                            <div class="tree-content" ref="treeContent">
+                                <div class="tg-head"><span class="tg-name">Расположение</span><span class="tg-count">ПК</span></div>
+                                <tree-form v-if="treeForm && treeForm.top" :level="0"></tree-form>
+                                <div v-if="!treeLoading && !treeError && treeRoots.length === 0" class="tree-empty">Расположения не найдены.</div>
+                                <div v-else-if="treeQuery.trim() && treeMatch.size === 0" class="tree-empty">Ничего не найдено.</div>
+                                <tree-node v-for="root in treeRoots" :key="root.id" :node="root" :level="0"></tree-node>
+                            </div>
+                        </div>
                     </div>
                     <!-- Действия со строкой — плашка справа от таблицы -->
                     <div v-if="treeHover" class="tree-actions" :style="{ top: treeHover.top + 'px', height: treeHover.height + 'px' }">
@@ -355,7 +360,7 @@
             </div>
             <!-- Отступ сверху — вне прокрутки: над шапкой ничего не проезжает -->
             <div class="history-page">
-                <div class="history-frame" :class="{ empty: !filteredHistory.length }">
+                <div class="history-frame" :class="{ empty: !filteredHistory.length, 'accent-head': accentHeaders.history }">
                 <div class="history-scroll">
                 <div v-if="!historyLoading && !historyError && historyItems.length === 0" class="empty-note">История пока пуста.</div>
                 <div v-else-if="historyQuery.trim() && filteredHistory.length === 0" class="empty-note">Ничего не найдено.</div>
@@ -443,7 +448,7 @@
                         <span v-for="login in cardVacuum" :key="login" class="copy-line"><span class="copy-val" title="Скопировать" @click="copyText(login)">{{ login }}<svg class="copy-ico" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="5.5" y="5.5" width="8" height="8" rx="1.5"/><path d="M10.5 3.5v-1a1 1 0 0 0-1-1h-6a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h1"/></svg></span></span>
                     </div>
                     <div class="card-section toggle-sec" :class="{ open: cardHistoryOpen }" @click="toggleCardHistory" :title="cardHistoryOpen ? 'Свернуть' : 'Развернуть'">
-                        <span>История</span><span v-if="cardHistory.length" class="sec-count" title="Изменений">{{ cardHistory.length }}</span>
+                        <span>История</span><span v-if="!cardLoading" class="sec-count" title="Изменений">{{ cardHistory.length }}</span>
                         <svg class="sec-chevron" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 6l4 4 4-4"/></svg>
                     </div>
                     <div v-if="cardHistoryOpen" class="card-sec-body">
